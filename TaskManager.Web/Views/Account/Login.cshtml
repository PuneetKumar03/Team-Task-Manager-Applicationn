@model TaskManager.Web.ViewModels.LoginViewModel
@{ ViewData["Title"] = "Login"; Layout = null; }
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Sign in — TaskManager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css" rel="stylesheet"/>
    <style>
        body{background:#f4f6fb;min-height:100vh;display:flex;align-items:center;justify-content:center;font-family:'Segoe UI',system-ui,sans-serif}
        .auth-card{background:#fff;border-radius:16px;border:1px solid #e8ecf4;padding:40px;width:420px}
        .logo-icon{width:40px;height:40px;background:#6366f1;border-radius:10px;display:flex;align-items:center;justify-content:center;color:#fff;font-size:20px}
        .form-control{border-radius:8px;border:1px solid #e2e8f0;font-size:13.5px;padding:9px 12px}
        .form-control:focus{border-color:#6366f1;box-shadow:0 0 0 3px rgba(99,102,241,.12)}
        .btn-primary{background:#6366f1;border:none;border-radius:8px;padding:10px;font-size:14px;font-weight:500}
        .btn-primary:hover{background:#4f46e5}
    </style>
</head>
<body>
<div class="auth-card">
    <div class="d-flex align-items-center gap-2 mb-4">
        <div class="logo-icon"><i class="bi bi-check2-square"></i></div>
        <span style="font-size:18px;font-weight:700;color:#1a1a2e">TaskManager</span>
    </div>
    <h5 style="font-weight:700;color:#1a1a2e;margin-bottom:4px">Welcome back</h5>
    <p style="font-size:13px;color:#94a3b8;margin-bottom:28px">Sign in to your account</p>

    <form asp-action="Login" method="post">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="alert alert-danger small rounded-3"></div>
        <div class="mb-3">
            <label asp-for="Email" class="form-label" style="font-size:13px;font-weight:500;color:#374151"></label>
            <input asp-for="Email" class="form-control" placeholder="you@example.com"/>
            <span asp-validation-for="Email" class="text-danger" style="font-size:12px"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Password" class="form-label" style="font-size:13px;font-weight:500;color:#374151"></label>
            <input asp-for="Password" class="form-control" placeholder="••••••••"/>
            <span asp-validation-for="Password" class="text-danger" style="font-size:12px"></span>
        </div>
        <div class="mb-4 form-check">
            <input asp-for="RememberMe" class="form-check-input"/>
            <label asp-for="RememberMe" class="form-check-label" style="font-size:13px;color:#64748b"></label>
        </div>
        <button type="submit" class="btn btn-primary w-100">Sign in</button>
    </form>
    <hr style="border-color:#f1f5f9;margin:24px 0"/>
    <p class="text-center mb-0" style="font-size:13px;color:#64748b">
        No account? <a asp-action="Register" style="color:#6366f1;text-decoration:none;font-weight:500">Create one</a>
    </p>
</div>
</body>
</html>